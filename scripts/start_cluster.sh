#!/usr/bin/env bash
set -euo pipefail

echo "Mock cluster startup is a no-op in this repository; configuration is file-driven."
echo "Use the loader with config/cluster.ini and config/schema.ini to exercise the system."
